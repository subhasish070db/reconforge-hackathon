# Set this before building the static GCS site. Obtain the value with:
# terraform -chdir=terraform output -raw backend_url
VITE_API_BASE_URL=https://YOUR_CLOUD_RUN_SERVICE_URL/api
# Required when publishing to https://storage.googleapis.com/<bucket>/index.html.
VITE_PUBLIC_BASE_PATH=/YOUR_GCS_BUCKET/
