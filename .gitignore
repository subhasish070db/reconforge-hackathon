# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Env / secrets
.env
.env.local

# Node
node_modules/
frontend/dist/
frontend/.vite/

# Terraform local state and provider cache (never commit cloud credentials or
# state, which can contain sensitive deployment values).
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/terraform.tfvars

# Data / uploads (runtime artifacts)
backend/uploads/
backend/data/*.db
*.sqlite3

# OS / editor
.DS_Store
.idea/
.vscode/
tasks.md
